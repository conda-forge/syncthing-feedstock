context:
  version: "2.1.3"

package:
  name: syncthing
  version: ${{ version }}

source:
  url: https://github.com/syncthing/syncthing/archive/v${{ version }}.tar.gz
  sha256: 177792c697f61fc25f02d8fc8923dfc57ebb35753bd92c8d32a73d553446d117

build:
  number: 1
  script:
    - go-licenses save ./cmd/syncthing --save_path library_licenses --ignore github.com/syncthing/ --ignore github.com/jackpal/go-nat-pmp
    - go run build.go -version v${{ version }} -no-upgrade build
    - if: unix
      then: install -D -m0755 -t "$PREFIX/bin" syncthing
      else: move syncthing.exe %LIBRARY_BIN%\

requirements:
  build:
    - go-licenses
    - ${{ compiler("go-cgo") }}
    - if: unix
      then:
        - ${{ compiler("c") }}
        - ${{ stdlib("c") }}
      else:
        - ${{ compiler("m2w64_c") }}
        - ${{ stdlib("m2w64_c") }}
    - if: osx
      then:
        - uutils-coreutils

tests:
  - script:
      - syncthing --version
      - syncthing --help
  - package_contents:
      strict: true
      bin:
        - syncthing

about:
  homepage: https://github.com/syncthing/syncthing
  repository: https://github.com/syncthing/syncthing
  summary: Open Source Continuous File Synchronization
  license: MPL-2.0
  license_file:
    - LICENSE
    - library_licenses/

extra:
  recipe-maintainers:
    - tao-j
    - XhstormR
